#ifndef MINITALK_H
#define MINITALK_H

#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <signal.h>
#include "mini_utils.c"

/*
void    safe_sigaction(int sig, struct sigaction sa);
void    safe_kill(pid_t id, int sig);*/
#endif
